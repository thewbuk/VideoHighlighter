"""
modules/narration — the prose passes written over a finished report.

Grouping only: these modules were loose at the top of ``modules/``
and import each other by full path as before. Nothing is re-exported
here, so a member is imported as ``modules.narration.<name>``.

    chapter_story
    clip_story
    llm_acceleration
    llm_discovery
    llm_models
    story_run
"""
