"""
modules/vision — detectors, tracking, pose, faces and the labels they produce.

Grouping only: these modules were loose at the top of ``modules/``
and import each other by full path as before. Nothing is re-exported
here, so a member is imported as ``modules.vision.<name>``.

    detection_backend
    expression_arc
    face_crops
    face_emotions
    face_scan
    label_store
    onnx_detector
    photo_library
    pose_backend
    r3d_onnx
    rtmpose_models
    tracking_backend
    training_preview
    yolox_models
"""
