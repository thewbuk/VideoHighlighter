"""
modules/segments — shot and chapter structure, and which spans get cut.

Grouping only: these modules were loose at the top of ``modules/``
and import each other by full path as before. Nothing is re-exported
here, so a member is imported as ``modules.segments.<name>``.

    auto_pipeline
    auto_segments
    chapter_compare
    chapter_speech
    chapters
    clip_quality
    compute_forbidden
    highlight_compare
    highlight_select
    highlight_swap
    manual_avoid
    motion_scene_detect_optimized
    reel_plan
    scene_cuts
    script_plan
    shot_look
    shot_place
    shot_type
    shot_window
    signal_combinations
    simple_run
"""
