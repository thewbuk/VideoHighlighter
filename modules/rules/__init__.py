"""
modules/rules — the composition rule engine.

Grouping only: these modules were loose at the top of ``modules/``
and import each other by full path as before. Nothing is re-exported
here, so a member is imported as ``modules.rules.<name>``.

    compose_events
    composition_signals
    rule_proposal
"""
