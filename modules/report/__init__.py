"""
modules/report — why a moment was chosen, and what the run could not say.

Grouping only: these modules were loose at the top of ``modules/``
and import each other by full path as before. Nothing is re-exported
here, so a member is imported as ``modules.report.<name>``.

    advisor
    analysis_ondemand
    analysis_plan
    analysis_stats
    detection_routes
    highlight_advice
    highlight_prose
    highlight_report
    sequence_findings
    spoken_evidence
    uncovered_claims
    vocabulary_gap
"""
